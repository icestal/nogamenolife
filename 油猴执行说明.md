# 油猴脚本执行说明 · 中英文名抓取(通用版 v2.0)

> 脚本不再写死 AppID,运行时弹窗让你输入/沿用。AppID 会自动记住,二次使用不用改代码。
> 抓的是 Steam 官方中英文名(数据源 `store.steampowered.com/api`),在 steamdb 页面运行。

## 前置条件

浏览器装有 **暴力猴(Violentmonkey)** 或 **油猴(Tampermonkey)** 扩展。

## 首次执行(全量 821 款)

1. 打开 **https://steamdb.info** 任意页面
2. 点扩展图标 → **添加新脚本**,清空默认内容,粘贴 `userscript_i18n.js` 全部代码 → Ctrl+S 保存
3. **刷新页面**,弹窗出现 → 输入 `new` → 回车
4. 第二个弹窗 → 打开项目目录 **`appids_all.txt`**,**全选复制**全部 821 个数字 → 粘进弹窗 → 回车
5. F12 → Console 看进度,约 5 分钟内完成,自动下载 `games_i18n.tsv`

## 以后增量更新(新增了几款)

1. 刷新 steamdb 页面 → 弹窗 → 输入 `new` → 回车
2. 把我给你的新增 AppID(几个数字)粘进弹窗 → 回车 → 几分钟抓完

## 说明

- **沿用上次**:弹窗直接回车 = 沿用已保存的 AppID(适合重跑全量)
- **AppID 存在浏览器本地**(GM_setValue),换机器/重装扩展需重新输入
- 输出格式:`appid \t 中文名 \t 英文名`(缺的留空),文件名 `games_i18n.tsv`

## 常见问题

- **没弹窗**:确认扩展已启用(图标有红点/数字角标),刷新一次
- **抓取报错**:F12 Console 截图给我;脚本内置每批 3 次重试
- **下载的 tsv 打不开乱码**:文件带 UTF-8 BOM,用记事本/VSCode 打开正常
- **中途想停**:刷新页面即停,下次重新跑
