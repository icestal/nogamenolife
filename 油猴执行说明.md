# 油猴脚本执行说明 · 中英文名全量抓取

> 目标:跑一次脚本,自动下载 `games_i18n.tsv`(821 款游戏的中英文名)。
> 耗时约 20 分钟,期间保持页面打开,挂机即可。

## 前置条件

浏览器需装有 **Tampermonkey(油猴)** 扩展。没有的话先装:
- Chrome/Edge: 扩展商店搜 "Tampermonkey" 安装

## 执行步骤

1. 打开 https://store.steampowered.com (无需登录,随便一个页面即可)
2. 点击浏览器右上角 Tampermonkey 图标 → **添加新脚本**
3. 清空编辑器里的默认内容,粘贴 `userscript_i18n.js` 的全部代码
4. 确认顶部配置区 `var ONLY_APPIDS = null;` 是 **null**(留空 = 全量抓)
5. Ctrl+S 保存
6. **刷新** store.steampowered.com 页面 —— 脚本会自动开跑
7. 按 F12 → Console 标签,可看到进度(共 821 款,每 50 款报一次)
8. **不要关闭页面**,约 20 分钟后脚本完成,自动下载 `games_i18n.tsv`

## 完成后

- 把下载的 `games_i18n.tsv` 文件放进项目目录 `d:\software\claude code\steamfamily\`(覆盖同名文件)
- 喊我合并进 games.json 并推送

## 常见问题

- **页面没反应**:确认脚本已启用(Tampermonkey 图标红色角标),再刷新一次
- **进度卡住/报错**:Console 里有红色报错截图给我即可;脚本内置 3 次重试
- **下载了但文件 0 字节**:检查 Console 是否显示"全部成功,无缺失"
- **想中途停**:刷新页面即停,已抓的不会丢(但不推荐,直接跑完最省事)

## 以后增量更新时(已抓过一次后)

新清单里只多了几款游戏时,把 `var ONLY_APPIDS = [新增的appid]` 填上,只抓这几款,几分钟搞定。
具体 appid 由我在对话里告诉你,不用自己找。
